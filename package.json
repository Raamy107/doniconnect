{
  "name": "doniconnect",
  "version": "1.0.0",
  "description": "DoniConnect — Donimalai & Sandur's trusted local platform for services, buying and selling",
  "scripts": {
    "dev": "npx http-server -p 3000",
    "build": "echo 'Static HTML build - no build step required'",
    "lint": "echo 'No linter configured for static HTML'"
  },
  "keywords": [
    "doniconnect",
    "donimalai",
    "sandur",
    "local-services"
  ],
  "dependencies": {
    "@vercel/speed-insights": "^1.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
